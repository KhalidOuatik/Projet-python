---
- name: Provisionnement de base et test de connexion
  hosts: all
  become: true
  tasks:
    - name: Vérifier la connexion (ping)
      ansible.builtin.ping:

- name: Installer Docker et Docker Compose
  hosts: all
  become: true
  tasks:
    - name: Installer les dépendances système
      ansible.builtin.apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
        state: present
        update_cache: true

    - name: Ajouter la clé GPG officielle de Docker
      ansible.builtin.apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present

    - name: Ajouter le dépôt Docker
      ansible.builtin.apt_repository:
        repo: deb [arch=amd64] https://download.docker.com/linux/ubuntu bionic stable
        state: present

    - name: Installer Docker
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
        state: present
        update_cache: true

    - name: Ajouter l'utilisateur courant au groupe docker
      ansible.builtin.user:
        name: "{{ ansible_user | default('azureuser') }}"
        groups: docker
        append: true

    - name: Installer Docker Compose
      ansible.builtin.get_url:
        url: https://github.com/docker/compose/releases/download/v2.29.2/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'
      register: docker_compose_download

    - name: Vérifier la version de Docker Compose
      ansible.builtin.command: docker-compose --version
      register: compose_version
      changed_when: false
      failed_when: compose_version.rc != 0

    - name: Afficher la version de Docker Compose
      ansible.builtin.debug:
        msg: "Docker Compose version: {{ compose_version.stdout }}"

- name: Déployer Flask (VM app)
  hosts: flask
  become: true
  roles:
    - flask

- name: Déployer Grafana/Prometheus/node_exporter (VM monitoring)
  hosts: monitoring_host
  become: true
  roles:
    - grafana
